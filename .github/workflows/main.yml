name: Cache Redis Config

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  cache-redis-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build
        run: |
          echo "Building..."
          echo "Cache Redis Config: Build successful!"